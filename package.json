{
	"name": "ts-mailgun",
	"version": "0.4.2",
	"description": "Mailgun wrapper for sending emails from NodeJS",
	"main": "ts-mailgun.js",
	"directories": {
		"test": "test"
	},
	"dependencies": {
		"@types/mailgun-js": "^0.22.4",
		"handlebars": "^4.7.3",
		"mailgun-js": "^0.22.0"
	},
	"devDependencies": {
		"@types/node": "^12.12.32",
        "typescript": "^3.8.3"
	},
	"scripts": {
		"test": "npm run build && jasmine --config=test/jasmine",
		"build": "npx tsc --p tsconfig.json",
		"build:spec": "npx tsc --p tsconfig.json && tsc --p tsconfig.spec.json",
		"doc": "npx typedoc --out docs src --excludeNotExported"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/StatelessStudio/ts-mailgun.git"
	},
	"author": "stateless-studio",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/StatelessStudio/ts-mailgun/issues"
	},
	"homepage": "https://github.com/StatelessStudio/ts-mailgun#readme",
	"keywords": [
		"typescript",
		"mailgun",
		"email"
	]
}
